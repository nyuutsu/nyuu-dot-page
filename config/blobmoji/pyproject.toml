[project]
name = "nyuu-page-blobmoji"
version = "0.1.0"
description = "Emoji font builder (subset Blobmoji to used glyphs)"
requires-python = ">=3.11"
dependencies = [
    "brotli",
    "fonttools",
    "nanoemoji",
    "resvg-cli",
]
